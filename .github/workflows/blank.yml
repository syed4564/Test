name: RustDesk Remote Access

on:
  workflow_dispatch:
    inputs:
      rustdesk_id:
        description: 'RustDesk ID for connection (optional)'
        required: false
        type: string
      rustdesk_password:
        description: 'RustDesk password (optional)'
        required: false
        type: string
      rustdesk_server:
        description: 'RustDesk Server address (optional for self-hosted)'
        required: false
        type: string

jobs:
  build:
    name: Install and Start RustDesk for Remote Access
    runs-on: windows-latest

    steps:
      - name: Downloading and Installing RustDesk
        run: |
          Write-Output "📥 Downloading RustDesk..."
          $rustdeskUrl = "https://github.com/rustdesk/rustdesk/releases/download/v1.1.9/rustdesk-1.1.9-x86_64.msi"
          $rustdeskInstallerPath = "$env:TEMP\\rustdesk-installer.msi"
          Invoke-WebRequest -Uri $rustdeskUrl -OutFile $rustdeskInstallerPath
          Write-Output "📦 Installing RustDesk..."
          Start-Process msiexec.exe -ArgumentList "/i `"$rustdeskInstallerPath`" /qn" -Wait
          Write-Output "✅ RustDesk installed."

      - name: Start RustDesk Session and Capture ID & Password
        run: |
          Write-Output "Starting RustDesk..."
          $rustdeskExePath = "C:\\Program Files\\RustDesk\\rustdesk.exe"
          Start-Process -FilePath "$rustdeskExePath" -ArgumentList "--no-ui" -Wait

          Write-Output "Waiting for RustDesk to generate the ID and Password..."
          Start-Sleep -Seconds 10  # wait for RustDesk to generate ID & Password
          
          # Simulate fetching ID and Password from RustDesk's log or any output method
          $rustdeskLog = "C:\\ProgramData\\RustDesk\\rustdesk.log"

          if (Test-Path $rustdeskLog) {
            Write-Output "Fetching RustDesk log..."
            $logContent = Get-Content $rustdeskLog

            # Extract ID and Password (this part depends on the RustDesk output format)
            $rustdeskID = ($logContent | Select-String -Pattern 'Remote ID:\s*(\d+)').Matches.Value
            $rustdeskPassword = ($logContent | Select-String -Pattern 'Password:\s*(\d+)').Matches.Value

            Write-Output "RustDesk ID: $rustdeskID"
            Write-Output "RustDesk Password: $rustdeskPassword"
          } else {
            Write-Error "❌ RustDesk log file not found!"
          }
      
      - name: Set Screen Resolution to 1366x768
        shell: cmd
        run: |
          @echo off
          set "nircmd_zip=%TEMP%\nircmd.zip"
          set "nircmd_dir=%TEMP%\nircmd"
          set "nircmd_exe=%nircmd_dir%\nircmd.exe"
          echo Downloading NirCmd...
          curl -L -o "%nircmd_zip%" "https://www.nirsoft.net/utils/nircmd.zip"
          echo Extracting NirCmd...
          powershell -Command "Expand-Archive -Path '%nircmd_zip%' -DestinationPath '%nircmd_dir%' -Force"
          if exist "%nircmd_exe%" (
              echo Setting resolution to 1366x768...
              "%nircmd_exe%" setdisplay 1366 768 32
              echo ✅ Resolution changed successfully.
          ) else (
              echo ❌ NirCmd executable not found.
          )

      - name: Software Installation (WinRAR + IDM + Tor Browser)
        run: |
          # Install WinRAR
          $winrarUrl = "https://www.rarlab.com/rar/winrar-x64-712.exe"
          $winrarPath = "$env:TEMP\\winrar-x64-712.exe"
          Invoke-WebRequest -Uri $winrarUrl -OutFile $winrarPath
          Start-Process -FilePath $winrarPath -ArgumentList "/S" -Wait
          Write-Output "✅ WinRAR installed."

          # Install IDM
          $idmUrl = "https://mirror2.internetdownloadmanager.com/idman642build41.exe"
          $idmPath = "$env:TEMP\\idm_setup.exe"
          Invoke-WebRequest -Uri $idmUrl -OutFile $idmPath
          if (Test-Path $idmPath) {
            Start-Process -FilePath $idmPath -ArgumentList "/skipdlgs" -Wait
            Write-Output "✅ IDM installed."
          } else {
            Write-Error "❌ IDM download failed."
          }

          # Tor Browser
          $dropboxUrl = "https://www.dropbox.com/scl/fi/faraacel5x4ssj5clo2j1/Browser.zip?rlkey=81rsd6nbwk2p95e0t3mpcirfi&st=3pnv7hia&dl=1"
          $zipPath = "$env:TEMP\\browser.zip"
          $extractPath = "$env:USERPROFILE\\browser"
          Invoke-WebRequest -Uri $dropboxUrl -OutFile $zipPath
          if (Test-Path $zipPath) {
            Expand-Archive -Path $zipPath -DestinationPath $extractPath -Force
            Write-Output "✅ Browser.zip extracted."
          }

          $firefox = "$extractPath\\Browser\\firefox.exe"
          $shortcut = "$env:USERPROFILE\\Desktop\\Tor.lnk"
          if (Test-Path $firefox) {
            $shell = New-Object -ComObject WScript.Shell
            $s = $shell.CreateShortcut($shortcut)
            $s.TargetPath = $firefox
            $s.WorkingDirectory = "$extractPath\\Browser"
            $s.IconLocation = "$firefox,0"
            $s.Save()
            Write-Output "✅ Tor Browser shortcut created."
          }

      - name: Install VPN Gate (via install_vpngate.bat)
        run: |
          $vpnURL = "https://www.dropbox.com/scl/fo/a6c0sqactnwr26cbbv1uc/APO1J80osQpo_8EoBOU3eJA?rlkey=s7wgg6ieuhlkr7wlxa9j9yij6&dl=1"
          $zipPath = "$env:TEMP\\vpn_bundle.zip"
          $extractPath = "$env:USERPROFILE\\Desktop\\vpn_bundle"
          $batPath = "$extractPath\\install_vpngate.bat"
          $vpnCheck = "C:\\Program Files\\SoftEther VPN Client\\vpncmgr.exe"
          Write-Output "📥 Downloading VPN bundle..."
          Invoke-WebRequest -Uri $vpnURL -OutFile $zipPath
          Expand-Archive -Path $zipPath -DestinationPath $extractPath -Force
          if (-Not (Test-Path $batPath)) { Write-Error "❌ install_vpngate.bat not found!"; exit 1 }
          Start-Process -FilePath "cmd.exe" -ArgumentList "/c `"$batPath`"" -WorkingDirectory $extractPath
          $maxWait = 300; $interval = 5; $elapsed = 0
          while (-not (Test-Path $vpnCheck) -and ($elapsed -lt $maxWait)) {
              Start-Sleep -Seconds $interval; $elapsed += $interval
              Write-Output "🔁 Checking... $elapsed sec"
          }
          if (Test-Path $vpnCheck) { Write-Output "✅ VPN Gate installed." } else { Write-Error "❌ VPN Gate timed out."; exit 1 }

      - name: Install VB-CABLE Virtual Audio Driver
        run: |
          $zip = "$env:TEMP\\VBCABLE.zip"
          $extract = "$env:TEMP\\VBCABLE"
          $url = "https://download.vb-audio.com/Download_CABLE/VBCABLE_Driver_Pack43.zip"
          Invoke-WebRequest -Uri $url -OutFile $zip
          Expand-Archive -Path $zip -DestinationPath $extract
          $setup = "$extract\\VBCABLE_Setup_x64.exe"
          if (Test-Path $setup) {
            Start-Process -FilePath $setup -ArgumentList "-i -h"
            Write-Output "⚠️ VB-CABLE installer launched."
          }
          Start-Service "AudioSrv"; Start-Service "AudioEndpointBuilder"
          Set-Service -Name "AudioSrv" -StartupType Automatic
          Set-Service -Name "AudioEndpointBuilder" -StartupType Automatic
          Write-Output "✅ Audio services running."

      - name: Clone Loop Repository and Run loop.bat
        run: |
          git clone https://github.com/Alex15969/Loop
          cd Loop
          cmd /c loop.bat
